import ServiceCard from "src/components/ServiceCard";

const transportandlogistics = (props) => {
  const { services } = props;
  return <ServiceCard services={services.TRANSPORT_AND_LOGISTICS} />;
};

export default transportandlogistics;
